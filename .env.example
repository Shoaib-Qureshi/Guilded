# NextAuth session signing secret — generate with: openssl rand -base64 32
AUTH_SECRET=""

# Supabase Postgres connection string (pooled).
# Supabase dashboard -> Project Settings -> Database -> Connection pooling -> URI.
# Looks like: postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres
DATABASE_URL=""
